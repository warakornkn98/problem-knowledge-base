# Base URL the browser uses to reach the API.
# With the dev-server proxy (and the production nginx) this stays as /api.
VITE_API_BASE_URL=/api

# Only used by the Vite dev server: where to proxy /api during `npm run dev`.
VITE_PROXY_TARGET=http://localhost:8080
